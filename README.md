# Weather Fox (portrait, light)

Only fox scenes for: clear/clouds/rain (morning/day/evening/night).
Run:
  flutter pub get
  flutter run -d edge

Your OpenWeather API key is already in lib/secrets.dart.
